#include<iostream>
#include<vector>
#include<string>

using namespace std;

int main(){
    string x, y;
    cin >> x >> y;

    int n = x.size();
    int m = y.size();

    vector<int> pre(n+1, 0), cur(n+1, 0);

    for(int i =0; i<=n; i++){
        pre[i] = 0;
    }

    for(int i =1)


}